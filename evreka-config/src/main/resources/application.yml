server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Perminand/evreka-config-CarXTestProject.git

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: config-server